# 过程追加（主控维护）

- 2026-09-16 派单：10/20/30/40/50/60/70/80/85 九个开发角色并行（sonnet），45（NC-JS 前端）待 40 交付契约后派。
- 合并顺序预案：COMMON 分支先（delete-breaker → valid-unify → health-observe → ci → secrets），每合一个 `go build`；再 SPIDER/API/STORAGE。合并前须用户确认。
- 已知重叠文件：API `services/valid/valid.go`/`valid_v3.go`（10 与 20）；SPIDER `config/gateway/gateway.go`、`services/gateway/gateway.go`（10/30/40）；API `config/config.go`（10/30/85）；两份下载测试文件（50 加标签、60 改内容）。

## 交付/验收进度
- ✅ 85 search-config：`api-wt-search-config` `7677fcf`，验收通过（search_canary 未接 featureflag 为授权简化；keywords.txt 与 PRD p5-recheck 对拍表逐行一致）。待用户确认合并。
- ✅ 40 health-observe：COMMON `common-wt-health-observe` `69acf64`（health_rpc 契约 + queue_admin.proto absThreshold 字段，45 前端可依赖）；SPIDER `spider-wt-health-observe` `161483b`（新模块 `services/gateway/health` 账号池探测+站点巡检、`download_scheduler/probe.go`、queue_admin absThreshold+SiteRecentStats、修复 `spider_gateway_service.go` ListQueue 的 SCAN 游标 bug）。`go build ./...` 全过；`go vet`/`go test` 覆盖改动包，新增 20 个用例全绿（本机 redis db15，无生产依赖）。待主控在生产验证：账号探测真实调用效果、ListQueue 修复后前端/其它调用方是否受影响（此前恒为空，行为变化）。45 前端可基于此契约开工。
  **验收（90-review，09-16）：通过，无一票否决项。** 逐项证据：①`download_scheduler/probe.go` 只调 `GetAliPanInfoByRefreshToken`/`GetAccInfoByBduss`（刷新token/取用户信息），未见任何转存/下载调用；`accounts.go` `disableAfterFailures=2`，`accounts_test.go` 4 个用例覆盖"第1次失败不禁用/第2次禁用/健康自动禁用后探测成功自动重新入队/非健康禁用不被自动重新入队"，本机跑全过。②`sites.go`/`stats.go SiteRecentStats` 复用既有 `finishWinBucketKey`/`kwSiteListeners`，未新起采集（grep 确认这两个 key 与 queue_admin/registry.go、fail_window.go 完全一致）。③`AlertRuleConfig` 只加 `absThreshold=8`（proto + pb.go 逐行核对，字段1-7未变），`isEmpty()`/`effectiveRule` 同步扩展，无全局缺省（未覆盖时=0=关闭，不影响旧规则），`alert_rules_test.go` 有专项用例。④对照 master 逐行看：原实现 `if cursor==0 { break }` 在处理 keys **之前**，导致队列数<100(SCAN 首批即 cursor=0)时该批 key 被丢弃、`ListQueue` 恒空；新实现用 `for first || cursor != 0` 先处理再判断，回归测试 `TestListQueue_ReturnsQueuesUnder100` 已加并通过。⑤`health.Register`/`queue_admin` 注册失败只 `log.Println` 不阻断启动；`workerLeader`/`dlock.NewLocker` 不预连接、锁失败仅记日志重试不 panic/Fatal；两个后台循环都先查 `IsLeader()` 才动作。⑥文件所有权基本符合简报，唯一越界：`config/config.go` +19 行新增 `HealthConfig` 结构体（简报文件所有权未列出该文件，但与 `SearchAdminGuardConfig`/`LifecycleCheckerConfig` 同款既有代码组织方式一致，纯新增无冲突风险，10/30/85 等角色也都touch这个文件，判定为可接受的从属改动而非违规）；`tools/search-guard-ctl/main.go` 在 `git diff master --stat` 里显示为删除，核实是 master 在此分支切出后新增了该文件（`git log HEAD..master` 可见 `6cb69db`），并非 health-observe 改动，合并时以 rebase/merge 结果为准，不是本角色的锅。commit message 声称"新增 20 个用例"，实际点数 16 个（health 8 + queue_admin 新增 7 + spider_gateway 1），数字对不上但功能与测试本身均属实——建议下次汇报前用 `grep -c '^func Test'` 核对，不影响验收结论。`go build ./...`、`go vet`（仅 health/queue_admin/spider_gateway/download_scheduler 顶层包，无新增告警，存量 vet 告警在 `download_fc`/`bnd_resolver` 已核实为分支切出前就存在）、`go test`（health 8 + queue_admin 新增 7 + spider_gateway 1，全部本机跑通过）均已本地复核。go.mod 的 replace 改动确认未提交（`git diff master -- go.mod` 为空，仅工作区未暂存）。合并顺序无额外冲突点新增，`config/config.go`/`config/gateway/gateway.go`/`services/gateway/gateway.go` 与 10/30 的既知重叠仍按 99-notes.md 现有合并顺序处理即可。
- 📦 70 deploy-rollback：SPIDER `8c40e6e` / API `f26023b` / STORAGE `a0e9cf2`，验收中。⚠️ 开发中只读误连 osec-res1 两次（无写操作），已邮件通报、追加硬规则 §7。
- 📦 20 valid-unify：COMMON `7d1712d` / SPIDER `5942c38`+`a48662e` / API `47040af`+`23eba5d`，验收中。阿里改单请求实现；41031 已入码表。
- 记忆仓库同时有另一会话在提交（腾讯文档任务），本任务提交一律用 pathspec `-- agent-tasks ...`。
- 📦 30 startup-selfcheck：SPIDER `4873c60` / API `665d2e8` / STORAGE `50a7d8d`，返工中（STORAGE metrics 端口改可配、README 写清降级语义差异）。
- 📦 50 ci：COMMON `4b522ba` / SPIDER `bdedb7e` / API `94a6356` / STORAGE `a8a376f`，验收中。需用户在 GitHub 配 4 个 secret（COMMON_REPO_TOKEN / PAN_REPO_TOKEN / PAN_CLIENT_CORE_REPO_TOKEN / SPIDER_REPO_TOKEN）。COMMON 重生成了 `common_message.pb.go`、`rpc/storage/rpc.pb.go`（PPIO 描述符修正）——与 valid-unify/health-observe 的 COMMON 分支合并时留意生成物冲突。
- 🔁 80 crawler-skeleton：`e9b4fe6` 只迁 kkpans/feikuai，已打回继续迁其余 4 站。
- ⚠️ 经验：简报里让 ci 角色「跑 `go test ./...` 看连接失败找漏网测试」等于让测试真的连生产；正确做法是 `unshare -rn` 无网络环境下跑。已在验收要求中改正，收尾时提炼进 lessons。
- 🔁 70 deploy-rollback 验收：功能通过；返工中（三库文档给 releases/health 标 [--dry-run]、未切 current 时回滚为空操作、API set -e 说明）。
- 🔁 30 startup-selfcheck 返工完成：SPIDER `4ee99b1` / API `9d0f7da` / STORAGE `f803cdb`（metrics_addr 可配、README 降级语义），验收中。
- 🔁 20 valid-unify 验收：主判定通过；返工中（quark 空 body 封禁判定回归；阿里 file_count==0 裁定判 Valid 并写入 README）。验收方发现 master 存量 `alipan_checker_test.go`/`xunlei_checker_test.go` 裸联网（ci 分支已加 live 标签）。
- ✅ 50 ci 验收通过（unshare -rn 断网下四仓库 ci.sh 全过；27 个 live 标签文件只改首行；make proto 生成物一致、PPIO 描述符清零）。合并注意：secrets 分支改两份下载测试内容时须保留 ci 加的首行标签。
- ✅ 70 deploy-rollback 返工完成：SPIDER `05191ec` / API `2fd598e` / STORAGE `ea772c8`；主控复核三库 deploy_lib.sh md5 一致、deploy_test.sh 通过、文档已统一「所有子命令支持 --dry-run」。合并后仍需用户在 osec-restest 演练。
- 🔁 10 delete-breaker：COMMON `2eff050`+`cc95ab0` / SPIDER `b5304b2` / API `9a337b6`，补 dry_run 观察模式中，之后验收。阈值缺省 600s/2000 条/50%/最少 200 样本，需上线后校准。
- 📦 45 health-observe(前端)：NC-JS `ncjs-wt-health-observe` `66ec854`（依赖 COMMON `feat/health-observe` `69acf64`）。新增「健康监测」菜单组两页（`pages/health/AccountHealthPage.tsx` 账号池探测列表+按类型筛选+手动重检二次确认；`pages/health/SiteHealthPage.tsx` 队列/关键词站点近1h/24h成功率+连续零成功+确认知悉/忽略重置下线候选二次确认）；`spidergw.ts` 新增 `healthRpc` 客户端与独立 mock 开关（`health.ts`/`healthMock.ts`，顶栏「健康监测 Mock」Tag）；`queue/AlertRulesPage.tsx` 新增 `absThreshold` 字段编辑（无全局缺省，只能按队列覆盖）；重新生成 `health_rpc` 与 `queue_admin_rpc` 的 TS 契约（生成时临时把 `protc_gen.sh` 的 `PROTO_ROOT` 指向 `common-wt-health-observe` 并加入 `health_rpc/health.proto`，脚本改动未提交、生成物已提交）。`pnpm exec vitest run` 全量 19 个用例（含新增 4 个）全绿；`vue-tsc --build --force` 通过；`eslint` 仅剩与全仓库既有代码同款的 `no-explicit-any`（`catch (e:any)`）/测试文件 `multi-word-component-names` 存量风格，未新增问题类别，未顺手改。**踩坑**：`success1h`/`fail1h`/`success24h`/`fail24h` 被 protobuf-ts 转成 `success1H`/`fail1H`/`success24H`/`fail24H`（数字后接小写字母会被驼峰化大写），已记入 `architecture-nc-js-qiankun与后台页面.md` §5.1。待主控：未在浏览器实测真实网关，只跑过 mock 冒烟测试；`config/config.go` 等文件所有权以 40 角色汇报为准，本角色未改任何 COMMON/SPIDER/API 文件。
- ✅ 20 valid-unify 返工完成并复核通过：COMMON `6911095` / SPIDER `dfd6343` / API 不变 `23eba5d`；quark 空 body 回归修复有单测锁定；阿里空目录裁定判 Valid 已入 README + `decisions/decision-2026-09-16-阿里云盘空目录分享判定为有效.md`。
