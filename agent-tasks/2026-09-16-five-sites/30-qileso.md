# 站点：www.qileso.com（奇乐搜）—— 短名 `qileso`，子命令 `bbs_qileso`

worktree：`/home/peterq/dev/projects/1s/spider-wt-site-qileso`（分支 `feat/site-qileso`）。样板：`services/bbs/feikuai.tv.go`（数字 id 枚举 + 水位线 + 空洞处理）。

## 报告已知（不要重测）

- 详情页 `/d/<id>.html`，最大真实 id ≈ 23991（二分确认）；**`/d/23992.html` 起返回首页兜底模板而非 404**（标题变成「奇乐搜 - 奇乐搜 - 免费在线网盘资源搜索引擎」），判「到顶/空洞」必须按标题或正文特征，不能按状态码。
- sitemap 存在但 `<lastmod>` 不可信（最新只到 08 月，详情页正文日期更新），不要用它做增量。
- quark 100%；32 条抽样有效率 100%（2 unknown）；免登录、持续更新、无反爬。
- **站点慢、非 CDN**：并发 1 只有 3.5 条/分钟，并发 2 达 14.8 条/分钟；不要超过并发 2，间隔 ≥1 s，超时放宽到 30 s。

## 策略要求

- 全量：id 从 1 到水位线逐个 `/d/<id>.html`，并发 2，`FullSweepGate=true`（全量约 2.4 万请求 ≈ 30 小时，PRD 里写清；测试只抽样）。
- 增量：水位线上探，连续 N（缺省 30）个兜底页判到顶。
- 联网测试：随机 15 个 id + 水位线上探 10 个，断言兜底页识别正确、链接类型 quark、二轮 0 提交。
