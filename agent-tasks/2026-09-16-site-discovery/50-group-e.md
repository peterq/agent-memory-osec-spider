# 分组 E —— 你要评估的站点

初筛（triage.py，2026-09-16）已知信息写在括号里，别再重复做。

**这组的特殊情况**：这三个站经代理池访问时都在约 5 s 处「Connection aborted / RemoteDisconnected」，
而本机直连正常返回 200。很可能是站点拒绝数据中心/代理出口 IP。生产爬虫**必须走代理池**（硬规则），
所以「代理池 IP 是否被拒」本身就是第 6 条可行性的关键判据，必须专门验证：

- 先用默认模式（走代理）对首页与 3 个详情页各请求 3 次，记录成功/失败次数与耗时，看是否稳定被拒。
- 若代理被稳定拒绝，用 `--no-proxy` 低速（并发 1、delay ≥1.5）完成规模/更新/有效率/登录墙的评估，
  但报告里第 6 条要写「代理池 IP 被拒，生产不可爬，除非换代理类型」并标为**不满足**（或证据不足）。
- 若代理有时成功，则是代理抖动，整套评估都走代理。

1. `https://pan.xiaozi.cc/`（直连 200；sitemap 90005 条（可能是 sitemap index 汇总，需核实真实条目）；首页无网盘链接；V2EX 汇总帖标注「需公众号」——重点验证第 5 条：详情页不带 cookie 能否直接拿到链接，还是要关注公众号取码）
2. `https://xiaojiwo.top/`（直连 200；sitemap 6303 条；首页无网盘链接；V2EX 标注「需登录」——重点验证第 5 条）
3. `https://pan.qianfan.app/`（直连 200；sitemap 1 条；首页无网盘链接；V2EX 标注「需公众号」。快速判定：找规模证据与登录墙，10 分钟内出结论）

报告写到 `260916/xiaozi.cc.md`、`260916/xiaojiwo.top.md`、`260916/qianfan.app.md`。
