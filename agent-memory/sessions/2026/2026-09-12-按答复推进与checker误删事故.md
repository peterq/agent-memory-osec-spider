---
title: 会话：按 open-questions 答复推进 P5，发现 lifecycle_checker 误删事故
type: session
status: active
created_at: 2026-09-12T23:25:00+08:00
updated_at: 2026-09-12T23:25:00+08:00
priority: medium
keywords: [P5, 事故, lifecycle_checker, cur窗口, 阶段D, search_canary, A']
summary: 09-12 晚按答复推进：:cur 两窗口取证不搬；A' 探测驱动就绪；阶段 D 改 API 侧灰度并派开发；推进中发现 checker 把 md5 当分享 id 误删 115.5 万条，修复 b888846，止血待人工
load: rarely
related:
  - agent-memory/lessons/failure-lifecycle_checker误传资源md5导致116万有效资源误删.md
  - agent-memory/decisions/decision-2026-09-12-阶段D改由API侧自动灰度分流.md
---

# 会话摘要 2026-09-12 22:10~23:30

## 完成事项
- `:cur` 两窗口（`t202608290000`/`t202608310000`）只读取证：DB 35,104 父全在 cur；55 个父（含 30 个大父）`parent_id` 计数 cur=legacy、long=0 → 不搬，问题关闭。
- 发现并定位事故：`lc-check` 总览 valid=0 → 代理池 `lifecycle_checker_quark` ok=0 而 `quark` 场景正常 → 容器统计 quark invalid=416,454 → 代码 `HandleTask` 传 `task.Id`。修复 + 单测 + 告警绝对阈值 SPIDER `b888846`，rollout §15 `fbb6912`。
- `docker stop` 与经隧道 `go run lc-check -params` 均被自动模式安全策略拦截，止血交人工。
- A' 只读探测驱动 `scripts/lc_legacy_probe_all.sh`（`fbb6912`/`c4d5c1f`，含重试与断点），xunlei 冒烟：缺失率 ≈0.13~0.16%。
- 阶段 D 决策落盘 + 简报 `agent-tasks/2026-09-12-p5-stage-d-canary/`，sonnet 子 Agent 开发中。
- 记忆：新 lesson/decision/backlog，tasks 拆分，6 个一次性 lesson 归档，lint 清零。

## 关键发现
- checker 自 09-05 起 100% 误判 quark/ali；bnd/xunlei 因未知响应全 error（零误删、零有效）。
- 旧链路 `clearExpire` 删旧索引无复核；`invalid_link_*` 无时间戳，需从 `res_lc_event` 反推范围。
- STORAGE Mongo `share_files` 按 url upsert、不受失效清理影响 → 恢复来源。

## 后续行动
见 `current/open-questions.md`（止血/部署/恢复决策）与 `current/tasks.md`「进行中」。
