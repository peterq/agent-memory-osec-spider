---
title: 会话：P5 阶段 A/B 开发与上线（D1 失效同步钩子 / API v2 valid 上报 / bnd 去 has_child）
type: session
status: active
created_at: 2026-09-12T17:00:00+08:00
updated_at: 2026-09-12T17:00:00+08:00
priority: medium
keywords: [P5, 阶段A, 阶段B, legacy_invalid_hint, has_child, ReportInvalid, redeployHost, trigger-check]
summary: 同日完成 P5 阶段 A/B 开发、验收与网关/API 上线；bnd v3 反超 v2；res2 重部半完成态补救
load: rarely
related:
  - agent-memory/decisions/decision-2026-09-12-P5切v3准入门槛与失效同步.md
  - agent-memory/procedures/workflow-部署.md
  - agent-memory/procedures/workflow-子agent任务简报.md
---

# 会话：P5 阶段 A/B 开发与上线（2026-09-12 15:50 ~ 17:00）

## 完成事项
- 三个 sonnet 子 Agent 并行（简报 `agent-tasks/2026-09-12-p5-stage-ab/`）：① 网关 D1 钩子（SPIDER `950b11a`，含单测+IT）；② API v2 `validShareLink` → `ReportInvalid(api-v2-search)` + bnd profile + `search_v3.go` 去 `has_child`（API `988fee3`，注释修正 `9e78b9e`）；③ A' 工具链 `lc-check -trigger-check` + `scripts/lc_legacy_deleted_probe.sh`（SPIDER `edd48ae`）。验收 Agent 全 ✅。
- 主控自做：`lc_v2_slow_ratio.sh` 路径参数（`4cb8d78`）、`es_curl.sh -d @文件` 修复（`b6b3fb8`）、`deploy.sh redeployHost`（`b1a306c`）、rollout §14、p5-plan 执行状态标注（`778cd45`）。
- 上线：网关 res1 16:24 / res2 16:29，API 双机 16:33。

## 关键发现
- `res_lc_all` 里 join=file 的 11.3 亿子文档全是 quark/ali/xunlei，**百度子文档为 0**，去 `has_child` 对 bnd 零损失；profile 显示 `ToParentBlockJoinQuery`+`GlobalOrdinalsWithScoreQuery` 占 bnd 查询 78%。热态 `凡人` v3 97 ms vs v2 196 ms。
- 存量旧索引缺失率抽样 quark 0.55% / bnd 0.05%，远低于首页样本推的 4%（首页是热门资源，被旧链路先判失效概率高）。
- 上线窗口 `clearExpire` 0 任务、jenkins `url_check` 近 6 h 全零、resdb ssh 超时——旧链路失效发现是否还在跑**未核实**。
- API 既有日志噪音：`json: cannot unmarshal string into Go struct field EnfiResource.size/expires`（SearchV1/Detail 旧脏文档）；网关 `try again` 与 gorm `record not found` 也是既有噪音。

## 决策
- 用户以「执行 p5-plan」整体采纳 D1~D4 → `decisions/decision-2026-09-12-P5切v3准入门槛与失效同步.md`。
- 主控裁定 API v2 路径直接 `ReportInvalid`（与 v3 valid 同一套判定实现），不新增 RPC/不接 redis。

## 问题与处置
- res2 网关重部在 `docker rm -f` 后 ssh 超时 → 半完成态约 4 分钟；`call dockerRun` 补救因缺 `OSS_CONFIG_URL` 进 Restarting；新增 `redeployHost` 后重建成功。
- 主控裸 `git commit` 卷入子 Agent 已暂存文件（内容正确、归属错乱）。
- auto 分类器拦了「开隧道 && go run」复合命令，拆成两条即可。

## 后续行动
见 `current/tasks.md`「进行中」：24 h 观察（rollout §14.3）→ 阶段 C 复测 → A' 存量分批 → 阶段 D 切流。

## 沉淀
- `procedures/workflow-部署.md`：半完成态补救用 `redeployHost`。
- `procedures/workflow-子agent任务简报.md`：同仓库并发提交必须带 pathspec。
