---
title: 会话：云端文档脚本从 userscripts 迁入 NC-JS
type: session
status: active
created_at: 2026-09-16T11:20:00+08:00
updated_at: 2026-09-16T11:20:00+08:00
priority: medium
keywords: [doc-cloud-spider, userscripts, 迁移, NC-JS, worktree]
summary: 云端文档脚本从个人仓库迁入 NC-JS apps/doc-cloud-spider, 验证全过, worktree 待确认合并
load: rarely
related:
  - agent-memory/decisions/decision-2026-09-16-云端文档脚本迁入NC-JS.md
---

# 会话摘要：云端文档脚本迁入 NC-JS（2026-09-16）

## 完成事项
- 从 `userscripts` HEAD（`7af9776`，用 `git archive` 避开用户未提交改动）取 `src/cloud/`、`vite.cloud.config.ts`、验证脚本，落到 NC-JS worktree `ncjs-wt-doc-cloud`（`feat/doc-cloud-spider`）的 `apps/doc-cloud-spider`。
- 目录重排：`docCloud.ts→src/main.ts`，站点实现进 `src/sites/`，`__tests__/fixtures→sites/__fixtures__`；import 与注释路径全部改掉，旧路径引用清零。
- 新 `package.json`（vite 6 / vitest 2 / vite-plugin-monkey 5 / vite-node）、`tsconfig.json`、`vite.config.ts`（monkey 仅 build）、README；根 README 与 `vitest.workspace.ts` 登记；spiderAdmin 注释改路径。
- SPIDER worktree `spider-wt-doc-cloud` 只改两处注释（`8c9b1c0`），`go build` 通过。
- 记忆仓库 `scripts/docspider/{e2e-local-fcchrome.sh,README.md}` 缺省路径改到新包。

## 验证
`tsc --noEmit` 通过；vitest 7 文件 59 用例通过；`vite build` 产物 52 KB（旧 54 KB，vite 5→6 差异）；`pnpm verify:qqdoc` 对 `DR1paWVp2cWxmc3NW` 1032 链接；`e2e-local-fcchrome.sh` 金山测试文档 193 链接 result final。

## 遇到的问题
- commit-msg 钩子拒绝 `Co-Authored-By`/邮箱（项目规则），去掉后提交。
- `pnpm install --filter` 在 worktree 走了 4 分钟（全量 lock 解析），lock 只多了新包与 `debug 4.3.7→4.4.0` 的良性升级。

## 后续行动
- 用户确认后：NC-JS `main` 与 SPIDER `master` fast-forward 合入、push；`apps/doc-cloud-spider` `pnpm build && pnpm upload`（人工）；删除两个 worktree。
- 待裁定：个人仓库里的过时副本是否删除；PC 端油猴调度器插件去留。

## 值得沉淀的经验
已提炼到决策文件；无新的失败经验。
