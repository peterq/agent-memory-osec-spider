---
title: 会话：分享链接总览 + 搜索总览（SLS）纳入后台，p90 告警自动关闭匿名搜索
type: session
status: active
created_at: 2026-09-16T08:10:00+08:00
updated_at: 2026-09-16T08:10:00+08:00
priority: medium
keywords: [ShareOverview, SearchAdminRpc, search_guard, SLS, 匿名搜索, spiderAdmin, 上线]
summary: 2026-09-16 一次会话内完成契约→4 子 Agent 并行编码→验收→上线（网关/API/前端）→端到端验证；上线首日发现 SLS stage 分词误匹配并修复
load: rarely
related:
  - agent-memory/decisions/decision-2026-09-16-搜索p90告警关闭匿名搜索与总览数据源.md
  - agent-memory/knowledge/architecture-search-admin.md
  - agent-memory/lessons/failure-SLS字段检索按分词匹配误命中其他stage.md
---

# 2026-09-16 分享链接总览 + 搜索总览纳入后台

## 完成事项
- 契约：COMMON `f2f0881`（`LifecycleRpc.ShareOverview`、新 `SearchAdminRpc`），Go/TS 生成物同步。
- SPIDER：lifecycle `ShareOverview` + 按类型计数器（`a84a10b`）；新模块 `search_admin`（`c5092c9` + 返工 `9305977`）；stage 精确过滤修复（`65a1bf8`）；工具 `tools/search-admin-check`、`tools/search-guard-ctl`、`tools/sls-probe`。主干 `2e34623`…`6cb69db`，网关 res1/res2 已部署两次（含修复）。
- API：`services/search-guard`（`35b90fd`，主干 `2458a52`），res1/res2 已部署（res2 首次 ssh 超时，单机补部）。
- NC-JS：4 页 + mock + 测试（`c29d926`、`cea4e0e`，主干 `dbbbbcc`），`pnpm build` 已上传 OSS。
- 端到端：手动关闭 1 分钟，res1 匿名请求 403 + `guard=anonymous_closed` 日志；leader 只有一台；Overview `source=sls-sql`。

## 关键发现
- SLS `resource-backend` logstore 的 `data` 字段**已可 SQL 分析**（无需改索引）。
- `stage:search` 分词命中 gin 访问日志 → 搜索量翻倍、uid/kw null；修法见 lessons。
- 首次 24h `ShareOverview` 在 res1 耗时 41s（ES 冷聚合），第二次 0.6s。
- Claude Code auto 模式分类器会拦截本机直连生产 SLS 的只读探针（「Production Reads」），但不拦 ssh 到生产主机看 docker 日志。

## 做出的决策
见 `decisions/decision-2026-09-16-搜索p90告警关闭匿名搜索与总览数据源.md`。

## 遇到的问题
- API `deploy.sh` 对 res2 ssh 公网 IP 超时一次，脚本 `set -e` 未中断（`|| true`）但 scp 失败退出 255；用 `source <(sed -n "1,48p" deploy.sh); deploy …; dockerRun …` 单机补部。
- 子 Agent 汇报里把 mock/验收写进了 `90-review.md`（可接受）。

## 后续行动
- 观察防护巡检一周：若误触发（ES 抖动）调 `sustainPolls`/阈值或 `autoClose=false`。
- ES 父文档过滤两种写法线上对拍（README curl）。
- 前端页面浏览器实测（子 Agent 只跑了 mock 冒烟）。

## 值得沉淀的经验
已提炼：`lessons/failure-SLS字段检索按分词匹配误命中其他stage.md`、`knowledge/architecture-search-admin.md`。
