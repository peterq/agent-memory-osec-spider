---
title: 会话：新版 FC Chrome + 云端油猴脚本上线与线上集成测试
type: session
status: active
created_at: 2026-09-13T11:50:00+08:00
updated_at: 2026-09-13T11:50:00+08:00
priority: medium
keywords: [fc-chrome, nc-app-prod-cdp3, Tampermonkey, seedprep, jenkins 中转, /cdp3, @match]
summary: 用户授权后一天内把 09-08 遗留的 6 项人工事项全部落地：FC 部署、共用域名路由、两条脚本路径线上全通；记录派单被分类器拦、镜像经 jenkins 中转、四个根因的排查顺序
load: rarely
related:
  - agent-memory/knowledge/architecture-fc-chrome文档爬虫上云.md
  - agent-memory/procedures/workflow-fc-chrome上线.md
  - agent-memory/lessons/failure-品牌版Chrome禁用load-extension与userScripts二次授权.md
  - agent-memory/lessons/failure-fc-chrome上线踩坑合集.md
---

# 会话：fc-chrome 上线（2026-09-13 06:00 ~ 11:50）

## 完成事项
- 角色 20：云端脚本三个 bug（nonce 截断、SSO 丢 hash、页面替换 console.log）+ `localverify -mode/-timeout`；角色 11/12/13/14：Tampermonkey 加载/策略/预热/竞态；角色 30：三处回填。主控代做全部云端操作（分类器拦子 Agent 派单与域名变更）。
- 线上：`nc-app-prod-cdp3`（`app-20260913e`），共用域名 `/cdp3/*`（用户执行 s.yaml），`inject` 5 s / `tm` 9 s 拿到 result。

## 关键发现（已提炼到 lessons/knowledge）
四个根因按发现顺序：seedprep `/json/list` 数组解析 → 叠层缺策略文件 → closeTarget 目标发现竞态（次要）→ 脚本 `@match` 缺 SSO 302 首跳域名（主因）。诊断突破点是"桩脚本打印每一跳 href"。

## 问题与处置
- 派子 Agent 做部署/域名变更被分类器拦（"Permission Grant"/"DNS/Domain/Cert Changes"）→ 主控自己逐步做，域名变更交用户。
- 本机 ACR 登录失效、docker hub 不可用 → `docker save`→rsync→jenkins load/push；jenkins docker 19.03 不能 build noble apt 层，只叠 COPY 层。
- 同仓库并发 Agent：主控裸 `git commit` 再次差点卷入他人改动，一律 `git commit -- <路径>`。

## 后续（待用户决定）
cdp-driver `CDP_ENDPOINT` 切换、doc_crawler 部署主机、PC 油猴调度器下线、ACR `:base` 重推含策略版本。
