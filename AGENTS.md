## 关于
风控技术部项目: 爬取网络上网盘资源, 为后续版权侵犯追责提供证据

### repos
- COMMON: `.` 本项目, 多个项目使用的工具类, rpc定义等. 同时用于管理 Agent 记忆和工作流
  - COMMON/agent-memory: 用于管理 Agent 记忆和工作流的目录, 由 Agent 自主维护
  - 实际存放位置: 独立仓库 `/home/peterq/dev/projects/peterq/agent-memory-osec-spider` (COMMON 内的 AGENTS.md / CLAUDE.md / agent-memory 均为指向该仓库的软链接, 记忆变更需在该仓库内 commit)

- STORAGE:  `../enfi-resource-storage` 存储服务, 主要负责爬取的资源的入库
- API: `../osec-resource-api` 给前端和其他部门提供资源查询接口服务
- SPIDER: `../osec-spider-go` 爬虫服务, 主要负责爬取网络资源, 调用 STORAGE 入库, 并将爬取的资源信息写入 ES, 供 API 查询
- NC-JS: `../nc-js` 前端mono repo
  - NC-JS/admin/* : 系统后台 由qiankun微前端框架管理的多子应用, 主要用于管理爬虫和资源


## Rules
向用户展示的信息使用 **中文**
Agent Memory使用 **中文**
代码注释使用 **中文**
为完成任务所用的脚本, 有价值的写入到项目中, 并记录文档, 以便开发者或其他 Agent 复用. 调用时通过项目中的路径调用以节省输出 token

## 下面是一份技能定义，适用于 AI Agent. 务必 **严格按照** `## 15. 自主进化循环` 工作流进行工作

```md
---
name: agent-memory
description: >
  通过 agent-memory/ 目录使用 Markdown 管理项目知识、会话经验、决策记录和可复用工作流程。
  Agent 必须在每次新会话开始时加载总览文件，并根据关键词和概要按需加载其他记忆文件。
  Agent 对该目录拥有完整的创建、读取、更新、整理、归档和重构权限。
  模型在开启思考模式时, 输出的 thinking 只涉及主要思路,核心要点, 而非和正文大量重复的内容. 避免浪费 token
---

# Agent Memory：可持续进化记忆系统

## 1. 目标

本技能用于让 Agent 在不同会话之间持续积累和复用经验。

Agent 必须通过 `agent-memory/` 目录保存以下信息：

- 项目基础信息
- 当前目标与进展
- 用户偏好
- 已确认的事实
- 重要决策及原因
- 可复用的工作流程
- 历史会话中的有效经验
- 曾经失败的方法及失败原因
- 待办事项、风险和未解决问题
- 其他有助于提高后续工作效率的信息

记忆系统应满足：

1. 新会话可以快速恢复上下文。
2. 总览内容足够精炼，不浪费大量 Token。
3. 详细知识可以按需加载，而不是一次性全部读取。
4. 所有记忆文件均为 Markdown。
5. 所有记忆文件都必须记录创建时间和更新时间。
6. 目录结构由 Agent 自主维护和优化。
7. 记忆内容必须区分事实、推断、偏好、决策和临时信息。
8. 过时信息必须被更新、标记或归档，不能无限堆积。
9. Agent 必须持续提炼高价值经验，并更新总览和索引。

---

## 2. 目录约定

默认目录：

```text
agent-memory/
├── 00-overview.md              # 必须加载：项目总览与当前状态
├── 01-index.md                 # 必须加载：所有记忆文件的关键词索引
├── 02-user-preferences.md      # 用户偏好与交互习惯
├── 03-project-context.md        # 项目背景、目标、边界和基础事实
├── current/
│   ├── goals.md                # 当前目标
│   ├── tasks.md                # 当前任务与进度
│   ├── risks.md                # 当前风险和阻塞
│   └── open-questions.md       # 尚未解决的问题
├── knowledge/
│   ├── domain-*.md             # 领域知识
│   ├── architecture-*.md       # 架构和系统说明
│   ├── api-*.md                # API、接口和协议
│   └── concept-*.md            # 概念、术语和规则
├── decisions/
│   ├── decision-*.md           # 重要决策记录
│   └── rejected-*.md           # 被否决的方案及原因
├── procedures/
│   ├── workflow-*.md           # 可复用工作流
│   ├── troubleshooting-*.md    # 问题排查流程
│   └── checklist-*.md          # 检查清单
├── lessons/
│   ├── success-*.md            # 成功经验
│   ├── failure-*.md            # 失败经验
│   └── patterns-*.md           # 可迁移模式
├── sessions/
│   └── YYYY/
│       └── YYYY-MM-DD-*.md     # 会话摘要
└── archive/
    └── YYYY/
        └── *.md                # 已归档记忆
```

### 目录管理原则

- 目录结构不是固定的，Agent 可以根据项目规模调整。
- 不允许为了形式而创建大量空文件夹。
- 文件数量较少时，可以合并相关内容。
- 单个文件过大时，应拆分为多个主题明确的 Markdown 文件。
- 文件名应使用中文、小写英文、数字和连字符，避免空格和特殊字符。
- 每个文件只承担一个主要主题。
- 不创建非 Markdown 类型的记忆文件。
- 临时草稿如无长期价值，不应写入持久记忆。

---

## 3. 文件元数据规范

每个 `.md` 文件的开头必须包含 YAML Front Matter：

```yaml
---
title: 文件标题
type: overview | index | context | preference | task | risk | question | knowledge | decision | procedure | lesson | session | archive
status: active | draft | deprecated | archived
created_at: 2026-08-24T10:30:00+08:00
updated_at: 2026-08-24T10:30:00+08:00
priority: critical | high | medium | low
keywords: [关键词1, 关键词2, 关键词3]
summary: 用一句话概括该文件的用途和核心内容
load: always | on-demand | rarely
related:
  - agent-memory/其他文件.md (当文件名不足以显示关联原因或用途时，请添加注释)
---
```

### 元数据要求

- `created_at`：文件首次创建时间，创建后不得修改。
- `updated_at`：每次内容发生变化时必须更新。
- 时间统一使用 ISO 8601 格式，并包含时区。
- 默认使用 `Asia/Shanghai`，例如：

  ```text
  2026-08-24T10:30:00+08:00
  ```

- `keywords` 用于检索和按需加载。
- `summary` 必须足够准确，使 Agent 不读取正文也能判断是否需要加载。
- `load: always` 仅用于必须在新会话启动时读取的文件。
- `related` 用于建立文件之间的关联。

---

## 4. 必须始终维护的文件

### 4.1 `00-overview.md`

这是整个记忆系统的总提炼文件。

每次新会话必须优先加载该文件。

该文件应保持精炼，建议控制在：

- 普通项目：800～2,000 字
- 复杂项目：最多 4,000 字
- 不应直接复制大量详细知识
- 只保留后续工作最需要的信息

推荐结构：

```md
---
title: 项目总览
type: overview
status: active
created_at: 2026-08-24T10:30:00+08:00
updated_at: 2026-08-24T10:30:00+08:00
priority: critical
keywords:
  - 项目名称
  - 核心目标
  - 当前阶段
summary: 项目的最小可用上下文和当前工作状态
load: always
related:
  - agent-memory/01-index.md
  - agent-memory/03-project-context.md
---

# 项目总览

## 1. 项目身份

- 项目名称：
- 项目类型：
- 项目目标：
- 当前阶段：
- 项目边界：
- 主要交付物：

## 2. 当前状态

- 当前正在处理：
- 已完成：
- 进行中：
- 下一步：
- 当前阻塞：
- 最近一次重要变化：

## 3. 核心事实

- 
- 
- 

只记录已经确认、且对后续工作有直接影响的事实。

## 4. 用户与协作偏好

- 输出语言：
- 输出风格：
- 技术偏好：
- 禁止或避免的做法：
- 验收标准：

详细内容见：
- `agent-memory/02-user-preferences.md`

## 5. 当前关键决策

- 决策：
- 原因：
- 影响：

详细内容见：
- `agent-memory/decisions/`

## 6. 高价值经验

- 
- 
- 

详细内容见：
- `agent-memory/lessons/`
- `agent-memory/procedures/`

## 7. 待解决问题

- 
- 
- 

详细内容见：
- `agent-memory/current/open-questions.md`

## 8. 快速加载指引

遇到以下主题时，加载对应文件：

| 主题 | 文件 |
|---|---|
| 项目背景 | `03-project-context.md` |
| 用户偏好 | `02-user-preferences.md` |
| 当前任务 | `current/tasks.md` |
| 风险问题 | `current/risks.md` |
| 历史决策 | `decisions/` |
| 工作流程 | `procedures/` |
| 失败经验 | `lessons/failure-*.md` |
| 领域知识 | `knowledge/` |
```

### 4.2 `01-index.md`

这是所有其他记忆文件的索引。

新会话必须在必要时加载此文件。若项目较小，也可以与 `00-overview.md` 一起始终加载。

推荐结构：

```md
---
title: 记忆索引
type: index
status: active
created_at: 2026-08-24T10:30:00+08:00
updated_at: 2026-08-24T10:30:00+08:00
priority: critical
keywords:
  - 索引
  - 文件
  - 关键词
summary: 根据关键词、主题和概要查找其他记忆文件
load: always
related:
  - agent-memory/00-overview.md
---

# 记忆索引

## 使用方法

先根据关键词、主题或文件概要查找候选文件，再按需读取正文。

## 文件索引

| 文件 | 类型 | 状态 | 关键词 | 概要 | 更新时间 |
|---|---|---|---|---|---|
| `03-project-context.md` | context | active | 项目、背景、目标 | 项目基础事实和范围 | 2026-08-24 |
| `current/tasks.md` | task | active | 任务、进度、待办 | 当前任务及完成状态 | 2026-08-24 |

## 关键词索引

### 项目背景

- 项目名称：`03-project-context.md`
- 项目目标：`03-project-context.md`
- 项目边界：`03-project-context.md`

### 用户偏好

- 输出格式：`02-user-preferences.md`
- 编程风格：`02-user-preferences.md`
- 沟通偏好：`02-user-preferences.md`

### 当前工作

- 当前任务：`current/tasks.md`
- 当前目标：`current/goals.md`
- 风险：`current/risks.md`
- 未解决问题：`current/open-questions.md`

### 决策

- 技术决策：`decisions/`
- 被否决方案：`decisions/rejected-*.md`

### 经验

- 成功经验：`lessons/success-*.md`
- 失败经验：`lessons/failure-*.md`
- 可复用模式：`lessons/patterns-*.md`

### 流程

- 工作流：`procedures/workflow-*.md`
- 排查流程：`procedures/troubleshooting-*.md`
- 检查清单：`procedures/checklist-*.md`

## 最近更新

按更新时间倒序记录最近发生变化的文件：

1. `文件路径`：变化概要
2. `文件路径`：变化概要
3. `文件路径`：变化概要
```

---

## 5. 新会话启动协议

每次新会话开始时，Agent 必须执行以下流程。

### 步骤 1：检查记忆目录

检查 `agent-memory/` 是否存在：

- 如果不存在，创建目录和最小文件集。
- 如果存在，检查核心文件是否完整。
- 如果核心文件缺失，优先补建。

最小文件集：

```text
agent-memory/
├── 00-overview.md
├── 01-index.md
├── 02-user-preferences.md
└── 03-project-context.md
```

### 步骤 2：加载总览

始终读取：

```text
agent-memory/00-overview.md
```

该文件是恢复上下文的第一来源。

### 步骤 3：加载索引

读取：

```text
agent-memory/01-index.md
```

如果总览已经足够完成当前任务，可以只读取索引的相关部分。

### 步骤 4：识别当前任务关键词

从用户当前请求中提取：

- 项目名称
- 功能名称
- 技术栈
- 任务类型
- 领域术语
- 相关实体
- 约束条件
- 错误信息
- 用户明确提及的历史事项

### 步骤 5：按需加载文件

使用索引中的关键词和概要信息匹配候选文件。

只加载与当前任务直接相关的文件，优先级如下：

1. 当前任务和目标
2. 相关项目背景
3. 相关用户偏好
4. 相关决策
5. 相关工作流程
6. 相关成功或失败经验
7. 更广泛的领域知识

不要因为文件存在就全部读取。

### 步骤 6：形成工作上下文

在开始执行任务前，内部整理：

```text
已知事实：
当前目标：
适用约束：
相关历史决策：
可复用流程：
潜在风险：
需要确认的问题：
```

如果记忆内容与用户当前明确指令冲突，以用户当前指令为准，并记录该变化。

---

## 6. 会话结束和持续学习协议

每次会话完成主要任务后，Agent 必须判断本次会话是否产生了长期价值。

### 应当写入记忆的内容

满足以下任一条件时，应写入或更新记忆：

- 用户明确说明了偏好。
- 项目产生了新的基础事实。
- 确定了重要技术或产品决策。
- 发现了可复用的解决方案。
- 发现了明确的失败原因。
- 当前任务、目标或风险发生变化。
- 形成了可以复用的工作流程。
- 用户纠正了 Agent 的错误。
- 某种方法在该项目中被验证有效或无效。
- 后续会话很可能需要再次使用该信息。
- 经过多轮交互才确定的有复用价值的信息。

### 不应写入的内容

不要保存：

- 没有长期价值的闲聊。
- 可以从代码或现有文件直接推导出的临时信息。
- 未验证的猜测，除非明确标记为推测。
- 重复已有内容。
- 过于具体且只适用于一次操作的中间过程。
- 敏感信息，除非用户明确要求保存且环境允许。

### 会话摘要

有长期价值的会话应创建：

```text
sessions/YYYY/YYYY-MM-DD-简短主题.md
```

推荐格式：

```md
---
title: 会话摘要：主题
type: session
status: active
created_at: 2026-08-24T18:00:00+08:00
updated_at: 2026-08-24T18:00:00+08:00
priority: medium
keywords:
  - 主题关键词
  - 任务关键词
summary: 本次会话完成的工作、关键决策和后续事项
load: on-demand
related:
  - agent-memory/00-overview.md
  - agent-memory/current/tasks.md
---

# 会话摘要

## 完成事项

- 

## 关键发现

- 

## 新增或改变的事实

- 

## 做出的决策

- 

## 遇到的问题

- 

## 后续行动

- 

## 值得沉淀的经验

- 
```

会话摘要不是最终知识库。应将其中的长期有效内容进一步提炼到：

- 项目上下文
- 决策记录
- 流程文件
- 成功经验
- 失败经验
- 当前任务或风险文件

---

## 7. 经验提炼规则

Agent 不应只记录“发生了什么”，还应提炼“以后如何做得更好”。

### 经验记录应尽量回答

1. 当时要解决什么问题？
2. 尝试了哪些方法？
3. 哪种方法有效？
4. 为什么有效？
5. 哪种方法无效？
6. 为什么无效？
7. 下次遇到类似问题，应优先采取什么行动？
8. 该经验适用的前提和边界是什么？

### 成功经验模板

```md
---
title: 成功经验：简短描述
type: lesson
status: active
created_at: 2026-08-24T18:00:00+08:00
updated_at: 2026-08-24T18:00:00+08:00
priority: medium
keywords:
  - 问题关键词
  - 技术关键词
summary: 在特定条件下解决某类问题的有效方法
load: on-demand
related:
  - agent-memory/procedures/相关流程.md
---

# 成功经验

## 问题

## 适用条件

## 推荐做法

## 原因

## 示例

## 注意事项

## 可迁移范围
```

### 失败经验模板

```md
---
title: 失败经验：简短描述
type: lesson
status: active
created_at: 2026-08-24T18:00:00+08:00
updated_at: 2026-08-24T18:00:00+08:00
priority: high
keywords:
  - 问题关键词
  - 错误关键词
summary: 某种方法在特定条件下失败的原因及规避方式
load: on-demand
related:
  - agent-memory/lessons/success-相关经验.md
---

# 失败经验

## 问题背景

## 失败方法

## 失败表现

## 根本原因

## 规避方法

## 下次行动建议

## 适用边界
```

---

## 8. 决策记录规则

重要决策必须单独记录，不应只存在于会话摘要中。

推荐文件：

```text
decisions/decision-YYYY-MM-DD-简短名称.md
```

格式：

```md
---
title: 决策：简短名称
type: decision
status: active
created_at: 2026-08-24T18:00:00+08:00
updated_at: 2026-08-24T18:00:00+08:00
priority: high
keywords:
  - 决策主题
  - 方案名称
summary: 关于某问题所采用的方案、原因和影响
load: on-demand
related:
  - agent-memory/03-project-context.md
---

# 决策：简短名称

## 背景

## 要解决的问题

## 备选方案

### 方案 A

优点：

缺点：

### 方案 B

优点：

缺点：

## 最终决策

## 决策原因

## 影响

## 复盘条件

在什么情况下需要重新评估该决策：

## 当前状态
```

如果决策被推翻：

1. 保留原文件。
2. 将 `status` 更新为 `deprecated`。
3. 记录替代决策的文件路径。
4. 在总览和索引中更新当前有效决策。

---

## 9. 内容可信度规范

记忆内容必须区分不同认知状态。

推荐使用标签：

- `[事实]`：已确认的信息。
- `[用户确认]`：用户明确说明或确认的信息。
- `[推断]`：根据已有信息推导出的结论。
- `[待确认]`：尚未验证的信息。
- `[临时]`：仅当前任务有效的信息。
- `[已废弃]`：历史上曾有效，但目前不再适用的信息。

不要把推测写成事实。

示例：

```md
- [事实] 项目使用 PostgreSQL。
- [用户确认] 用户希望所有接口文档使用中文。
- [推断] 当前性能瓶颈可能来自数据库连接池。
- [待确认] 生产环境是否启用了缓存。
```

---

## 10. 更新规则

### 修改已有文件时

必须：

1. 保留原始创建时间。
2. 更新 `updated_at`。
3. 检查 `summary` 是否仍然准确。
4. 检查 `keywords` 是否需要增加或删除。
5. 检查 `related` 是否需要更新。
6. 如内容已过时，标记相应状态。
7. 如修改影响总览或索引，必须同步更新。

### 更新总览时

只有以下内容适合进入 `00-overview.md`：

- 当前阶段
- 当前目标
- 当前任务
- 关键项目事实
- 重要约束
- 当前有效决策
- 最近的重要变化
- 最值得复用的经验
- 关键文件的导航信息

以下内容不应直接堆入总览：

- 完整技术文档
- 很长的会话记录
- 所有历史细节
- 已经失效的方案
- 与当前目标无关的知识

### 更新索引时

当发生以下情况时必须更新 `01-index.md`：

- 新增文件。
- 删除文件。
- 文件移动。
- 文件重命名。
- 文件主题变化。
- 关键词变化。
- 文件状态变化。
- 文件概要变化。
- 重要文件更新时间发生变化。

---

## 11. Token 控制策略

为了平衡上下文恢复速度和 Token 消耗，遵循以下规则：

### 默认加载层级

#### 第一层：始终加载

```text
agent-memory/00-overview.md
```

必要时加载：

```text
agent-memory/01-index.md
```

#### 第二层：任务相关加载

根据当前任务加载：

- `03-project-context.md`
- `02-user-preferences.md`
- `current/` 下相关文件
- 相关决策
- 相关流程

#### 第三层：深入加载

只有在确实需要时加载：

- 历史会话
- 详细领域知识
- 失败经验
- 已归档内容
- 与当前任务弱相关的资料

### 文件长度建议

- `00-overview.md`：尽量不超过 4,000 字。
- `01-index.md`：尽量只包含导航信息，不复制正文。
- 普通记忆文件：建议不超过 3,000～6,000 字。
- 超过 8,000 字时，应考虑拆分。
- 会话摘要应尽量控制在 500～2,000 字。

### 避免重复

- 总览只写摘要，不复制详细内容。
- 索引只写关键词和概要，不复制正文。
- 会话文件只记录过程摘要，不重复完整知识。
- 决策文件只记录决策本身及其依据。
- 详细说明放在最合适的单一文件中，其他文件通过链接引用。

---

## 12. 自动整理和归档

Agent 应定期检查记忆目录，主动进行以下整理：

### 合并

发现多个文件内容高度重复时：

1. 合并为一个主题明确的文件。
2. 保留更完整、更准确的内容。
3. 在旧文件中记录替代文件。
4. 更新索引和相关引用。

### 拆分

发现一个文件包含多个独立主题，或文件过长时：

1. 按主题拆分。
2. 为每个新文件添加完整元数据。
3. 更新总览和索引。
4. 删除或归档原文件。

### 归档

以下内容可以移动到 `archive/YYYY/`：

- 已完成且不会继续使用的任务。
- 已被新决策替代的决策。
- 过时的会话摘要。
- 已不再适用的临时信息。
- 与当前项目阶段无关的历史资料。

归档前必须：

- 设置 `status: archived`。
- 保留原始 `created_at`。
- 更新 `updated_at`。
- 在原位置或索引中留下替代路径。
- 确认不会破坏其他文件的引用。

### 删除

只有在以下情况下才删除文件：

- 文件内容完全重复。
- 文件内容明显错误且没有历史价值。
- 文件由误操作产生。
- 用户明确要求删除。

如果内容虽然过时但具有复盘价值，应归档而不是删除。

---

## 13. 冲突处理

当不同记忆文件之间存在冲突时，按以下优先级判断：

1. 用户当前明确指令。
2. 最近一次用户确认的信息。
3. 当前有效的决策文件。
4. 最近更新时间较新的项目上下文。
5. 历史会话摘要。
6. 未确认的推断和临时记录。

发现冲突时：

1. 不要静默覆盖重要信息。
2. 记录冲突及其来源。
3. 以更高优先级信息为准。
4. 更新相关文件的状态。
5. 必要时创建新的决策记录。
6. 在总览中保留当前有效结论。

---

## 14. 敏感信息与安全

Agent 不应主动保存以下内容：

- 密码
- API Key
- 私钥
- 访问令牌
- 身份证号码
- 银行卡信息
- 未经授权的个人隐私
- 不必要的敏感业务数据

如果必须记录，应保存：

- 脱敏后的信息
- 变量名
- 配置位置
- 使用方式
- 安全注意事项

例如，不要保存：

```text
API_KEY=真实密钥
```

应保存：

```text
API Key 存储于环境变量 `PROJECT_API_KEY`，不要写入仓库或记忆文件。
```

---

## 15. 自主进化循环

Agent 应持续执行以下闭环：

```text
读取总览
  ↓
识别当前任务关键词
  ↓
按索引加载相关记忆
  ↓
执行当前任务
  ↓
识别新事实、决策和经验
  ↓
更新详细记忆文件
  ↓
提炼高价值内容到总览
  ↓
更新索引、关键词和概要
  ↓
归档过时内容
```

每次完成任务后，至少检查：

- 是否出现了新的项目事实？
- 是否出现了新的用户偏好？
- 是否做出了重要决策？
- 是否发现了可复用方法？
- 是否验证了某种方法无效？
- 是否改变了当前任务或风险？
- 是否需要更新 `00-overview.md`？
- 是否需要更新 `01-index.md`？

---

## 16. 最低执行要求

如果时间、工具或上下文有限，至少执行以下操作：

### 新会话

1. 读取 `agent-memory/00-overview.md`。
2. 根据任务关键词查找 `01-index.md`。
3. 加载至少一个最相关的详细文件，若存在。

### 任务完成后

1. 判断是否产生长期有效信息。
2. 有则更新或创建相应 `.md` 文件。
3. 修改文件时更新 `updated_at`。
4. 如影响导航，更新 `00-overview.md` 和 `01-index.md`。

### 不允许的行为

- 忽略已有总览，完全从零开始工作。
- 将所有历史文件一次性加载。
- 创建没有元数据的记忆文件。
- 修改 `created_at` 伪装文件创建时间。
- 把未经确认的推测记录为事实。
- 让索引长期指向不存在的文件。
- 只记录过程，不提炼可复用经验。
- 让总览无限膨胀，失去快速恢复上下文的作用。
```
